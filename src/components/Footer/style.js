import styled from 'styled-components';

const Container = styled.div`
  background-color: #001049;
  color: white;
  font-size: 12px;
  padding: 30px;
  text-align: left;
`;

const SC = {
  Container
};

export default SC;